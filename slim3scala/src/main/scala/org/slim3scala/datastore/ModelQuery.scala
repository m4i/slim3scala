package org.slim3scala.datastore
/*

import com.google.appengine.api.datastore.Key
import com.google.appengine.api.datastore.Transaction

import org.slim3.datastore.ModelMeta

class ModelQuery[M](modelMeta: ModelMeta[M])
    extends org.slim3.datastore.ModelQuery[M](modelMeta) {

  def this(modelMeta: ModelMeta[M], ancestorKey: Key) = {
    this(modelMeta)
    setUpQuery(modelMeta.getKind, ancestorKey)
  }

  def this(tx: Transaction, modelMeta: ModelMeta[M], ancestorKey: Key) = {
    this(modelMeta, ancestorKey)
    setTx(tx)
  }
}
*/
